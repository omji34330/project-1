# Google Gemini API key — required. Get one at https://aistudio.google.com/app/apikey
# This file is a template: copy it to `.env` and fill in real values.
# .env is gitignored and must never be committed.
GEMINI_API_KEY=AQ.Ab8RN6JV5CawRMJuF02ysCPblHaHbe-dU9qoVmfa8acfzS5fqQ
# Optional: override the Gemini model used for chat.
GEMINI_MODEL=gemini-2.5-flash

# Optional: the frontend origin allowed to call this API (CORS).
FRONTEND_ORIGIN=http://localhost:5173
