# Base URL of the FastAPI backend used by the chatbot.
# In development the Vite dev server proxies /api to this target already
# (see vite.config.ts), so this only needs to change for production builds
# where the frontend and backend are deployed to different hosts.
VITE_API_BASE_URL=http://localhost:8000
